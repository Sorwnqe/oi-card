import * as React from 'react';

import Layout from '@/Layout/Layout';

import Content from './components/Content';

const Home = () => {
  return (
    <Layout>
      <Content />
    </Layout>
  );
};

export default Home;
